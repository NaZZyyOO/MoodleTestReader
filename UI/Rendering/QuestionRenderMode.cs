namespace MoodleTestReader.UI.Rendering
{
    /// <summary>
    /// Режим відображення питання.
    /// Play — проходження тесту (контроли активні).
    /// Review — огляд виконаного тесту (контроли тільки для перегляду).
    /// </summary>
    public enum QuestionRenderMode
    {
        Play,
        Review
    }
}